const SectionHeader = ({ children }) => {
  return (
    <h1 className="text-white text-sm pb-8 xl:px-0 lg:px-0 md:px-[10px] xs:px-0">
      {children}
    </h1>
  );
};

export default SectionHeader;
